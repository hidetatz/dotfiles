snippet lcm
options head
    long long gcd(long long a, long long b) {
     if (a == 0)
       return b;
     return gcd(b % a, a);
    }

    long long lcm(long a, long b) { return (a * b) / gcd(a, b); }
